let user = {
    user_Name: "Rahul",
    loc: 'Wayanad'
}
let emp = {}

Object.keys(user).length > 0 ? console.log("Not Empty Object")
    : console.log("Empty")
/*
let x = Object.keys(user)
console.log(x.length) */